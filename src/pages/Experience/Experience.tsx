import { SectionContainer } from 'components/Container/SectionContainer';

export const Experience = (): JSX.Element => {
  return <SectionContainer titleTextKey="pages.experience">Experience</SectionContainer>;
};
